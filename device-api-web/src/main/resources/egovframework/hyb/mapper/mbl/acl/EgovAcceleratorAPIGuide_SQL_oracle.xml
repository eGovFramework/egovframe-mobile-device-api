<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="acceleratorAPIDAO">

	<resultMap id="acceleratorInfo" type="egovframework.hyb.mbl.acl.service.AcceleratorAPIVO">
		<result property="sn" column="SN"/>
		<result property="uuid" column="UUID"/>
		<result property="useYn" column="USEYN"/>
	</resultMap>

	<insert id="insertAcceleratorInfo">
		<selectKey keyProperty="sn" resultType="int" order="BEFORE">
		   SELECT (IFNULL(MAX(SN), 0)+1) sn from ACCELERATOR WHERE UUID = #{uuid}
		</selectKey>
		
		INSERT INTO ACCELERATOR 
			( SN
			  , UUID
			  , USEYN )
		VALUES ( #{sn}
			  , #{uuid}
			  , #{useYn})
	</insert>
	
	<update id="updateAcceleratorInfo">
		UPDATE ACCELERATOR 
		SET SN=#{sn}
			, UUID=#{uuid}
			, USEYN=#{useYn}
		WHERE SN=#{sn} AND UUID=#{uuid}
	</update>
	
	<delete id="deleteAcceleratorInfo">
		UPDATE ACCELERATOR 
		SET USEYN='N'
		WHERE SN=#{sn} AND UUID=#{uuid}
	</delete>
	
	<select id="selectAcceleratorInfo" resultMap="acceleratorInfo">
		SELECT
			SN, UUID, USEYN
		FROM ACCELERATOR 
		WHERE SN=#{sn} AND UUID=#{uuid}
	</select>
	
	<select id="selectAcceleratorInfoList" parameterType="egovframework.hyb.mbl.acl.service.AcceleratorAPIVO" resultMap="acceleratorInfo">
		SELECT
			SN, UUID, USEYN
		FROM ACCELERATOR
		WHERE USEYN='Y' AND UUID=#{uuid}
		ORDER BY SN DESC
	</select>

</mapper>
