<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fileReaderWriterAPIDAO">

	<resultMap id="fileReaderWriterInfo" type="egovframework.hyb.mbl.frw.service.FileReaderWriterAPIVO">
		<result property="sn" column="SN"/>
		<result property="uuid" column="UUID"/>
		<result property="useYn" column="USEYN"/>
	</resultMap>

	<insert id="insertFileReaderWriterInfo">
		<selectKey keyProperty="sn" resultType="int" order="BEFORE">
		   SELECT (IFNULL(MAX(SN), 0)+1) sn from FILE_READER_WRITE WHERE UUID = #{uuid}
		</selectKey>
		
			INSERT INTO FILE_READER_WRITE
				( SN
				  , UUID
				  , FILE_SN
				  , FILE_NM
				  , FILE_COURS
				  , FILE_TYPE
				  , UPDT_DT
				  , FILE_SIZE
				  , USEYN )
			VALUES ( #{sn}
				  , #{uuid}
				  , #{fileSn}
				  , #{fileNm}
				  , #{fileStreCours}
				  , #{fileType}
				  , SYSDATE()
				  , #{fileSize}
				  , #{useYn})
		
	</insert>
	
	<select id="selectFileReaderWriterInfoList" resultType="egovframework.hyb.mbl.frw.service.FileReaderWriterAPIVO">

			SELECT
				SN, UUID, FILE_SN fileSn, FILE_NM fileNm, 
				FILE_COURS fileStreCours, FILE_TYPE fileType, UPDT_DT updtDt, 
				FILE_SIZE fileSize, USEYN useYn
			FROM FILE_READER_WRITE
			WHERE USEYN='Y' AND UUID = #{uuid}
			ORDER BY SN DESC
	</select>
	
	<select id="selectFileReaderWriterInfo" resultType="egovframework.hyb.mbl.frw.service.FileReaderWriterAPIVO">

			SELECT
				A.SN, A.UUID, A.FILE_SN fileSn, 
				A.FILE_NM, A.FILE_COURS, A.FILE_TYPE, 
				A.UPDT_DT, A.FILE_SIZE, A.USEYN,
				B.STRE_FILE_NM streFileNm, B.ORIGNL_FILE_NM orignlFileNm
			FROM FILE_READER_WRITE A JOIN FILE_DETAIL_INFO B USING(FILE_SN)
			WHERE USEYN='Y' AND UUID = #{uuid} AND FILE_SN = #{fileSn}
			ORDER BY SN DESC
	</select>
	
	<delete id="deleteFileReaderWriterInfo">
			
			UPDATE FILE_READER_WRITE 
			SET USEYN='N'
			WHERE SN = #{sn} AND UUID = #{uuid}
		
	</delete>

</mapper>
