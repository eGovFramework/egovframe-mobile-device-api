<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="deviceAPIDAO">

	<resultMap id="deviceInfo" type="egovframework.hyb.mbl.dvc.service.DeviceAPIVO">
		<result property="sn" column="SN"/>
		<result property="uuid" column="UUID"/>
		<result property="useYn" column="USEYN"/>
	</resultMap>

	<insert id="insertDeviceInfo">
		<selectKey keyProperty="sn" resultType="int" order="BEFORE">
		   SELECT (IFNULL(MAX(SN), 0)+1) sn from DEVICE
		</selectKey>
		
		INSERT INTO DEVICE 
			( SN
			  , UUID
			  , USEYN )
		VALUES ( #{sn}
			  , #{uuid}
			  , #{useYn})
	</insert>
	
	<select id="selectDeviceInfoList" parameterType="egovframework.hyb.mbl.dvc.service.DeviceAPIVO" resultMap="deviceInfo">
		SELECT
			SN, UUID, USEYN
		FROM DEVICE
		WHERE USEYN='Y'
		ORDER BY SN DESC
	</select>

</mapper>
