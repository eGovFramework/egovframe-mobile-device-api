<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="networkAPIDAO">

	<resultMap id="networkInfo" type="egovframework.hyb.mbl.nwk.service.NetworkAPIVO">
		<result property="sn" column="SN"/>
		<result property="uuid" column="UUID"/>
		<result property="networktype" column="NTWRK_TYPE"/>
		<result property="useYn" column="USEYN"/>
	</resultMap>

	<insert id="insertNetworkInfo">
		<selectKey keyProperty="sn" resultType="int" order="BEFORE">
		   SELECT (IFNULL(MAX(SN), 0)+1) sn from NETWORK WHERE UUID = #{uuid}
		</selectKey>
		
		INSERT INTO NETWORK 
			( SN
			  , UUID
			  , NTWRK_TYPE
			  , USEYN )
		VALUES ( #{sn}
			  , #{uuid}
			  , #{networktype}
			  , #{useYn})
	</insert>
	
	<update id="updateNetworkInfo">
		UPDATE NETWORK 
			SET SN = #{sn}
				, UUID = #{uuid}
				, NTWRK_TYPE = #{networktype}
				, USEYN = #{useYn}
		WHERE SN = #{sn} AND UUID = #{uuid}
	</update>
	
	<delete id="deleteNetworkInfo">
		DELETE FROM NETWORK 
		WHERE SN=#{sn} AND UUID=#{uuid}
	</delete>
	
	<select id="selectNetworkInfo" resultMap="networkInfo">
		SELECT
			SN, UUID, NTWRK_TYPE, USEYN
		FROM NETWORK 
		WHERE SN=#{sn} AND UUID=#{uuid}
	</select>
	
	<select id="selectNetworkInfoList" parameterType="egovframework.hyb.mbl.nwk.service.NetworkAPIDefaultVO" resultMap="networkInfo">
		SELECT
			SN, UUID, NTWRK_TYPE, USEYN
		FROM NETWORK
		WHERE USEYN='Y' AND UUID = #{uuid}
		ORDER BY SN DESC
	</select>
	
	<select id="selectNetworkInfoListTotCnt" parameterType="egovframework.hyb.mbl.nwk.service.NetworkAPIDefaultVO" resultType="int">
		SELECT COUNT(*) totcnt
		FROM NETWORK
		WHERE USEYN='Y' AND UUID = #{uuid}
	</select>

</mapper>
